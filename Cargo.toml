[package]
name = "dprx"
version = "0.1.0"
edition = "2024"

[dependencies]
mio = { version = "1.0.3", features = ["os-poll", "net"] }
nix = { version = "0.29.0", features = ["process"] }
socket2 = "0.5.9"
